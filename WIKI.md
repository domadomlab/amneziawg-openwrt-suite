# Wiki: AmneziaWG 2.0 на OpenWrt (aarch64)

Данное руководство описывает архитектуру, установку и эксплуатацию системы AmneziaWG 2.0 с поддержкой маскировки трафика и автоматическим восстановлением доступа.

## 🚀 Архитектура решения
Система построена на базе **user-space** реализации драйвера, что обеспечивает совместимость с любыми версиями ядер OpenWrt без необходимости компиляции модулей ядра (`kmod-amneziawg`).

### Основные компоненты:
1.  **`amneziawg-go`**: Основной бинарный файл драйвера. Работает через `/dev/net/tun`.
2.  **`awg-new`**: Утилита управления (аналог `wg`), поддерживающая расширенные параметры Amnezia (Jc, S1, H1-H4, I1).
3.  **`amneziawg-start.sh`**: Интеллектуальный скрипт запуска. Выполняет парсинг конфига, настройку маршрутов, Firewall и запускает проверку здоровья.
4.  **`amneziawg-health-check.sh`**: "Предохранитель". Мониторит трафик и доступность Google. При сбое автоматически вызывает Rollback.
5.  **`amneziawg-stop.sh`**: Скрипт полной остановки и восстановления исходного состояния сети.

## 🛠️ Установка и развертывание

### Требования к системе:
- Архитектура: `aarch64` (Mediatek MT7987, Flint 2, Beryl AX и др.).
- Пакеты: `kmod-tun`, `ip-full`, `iptables`, `luci-base`.

### Ручной деплой:
1. Скопируйте бинарные файлы в `/usr/bin/` и дайте права:
   ```bash
   chmod +x /usr/bin/amneziawg-go /usr/bin/awg-new
   ```
2. Скопируйте скрипты управления в `/usr/bin/` и дайте права:
   ```bash
   chmod +x /usr/bin/amneziawg-start.sh /usr/bin/amneziawg-stop.sh /usr/bin/amneziawg-health-check.sh
   ```
3. Создайте директорию конфигурации:
   ```bash
   mkdir -p /etc/amneziawg/
   ```
4. Разместите ваш конфиг по пути `/etc/amneziawg/awg0.conf`.

## 🛡️ Система Health Check & Rollback
Это критически важная часть системы, предотвращающая потерю доступа к роутеру.

- **Как это работает**: Сразу после старта запускается фоновый процесс проверки.
- **Критерии успеха**: 
  1. Интерфейс `awg0` существует.
  2. Получен входящий трафик (Rx bytes > 0) в течение 15 секунд.
  3. Проходит пинг до `8.8.8.8` через VPN-туннель.
- **Откат (Rollback)**: Если хотя бы одно условие не выполнено, система автоматически выполняет `amneziawg-stop.sh`, возвращая роутеру интернет через обычного провайдера.

## 🌐 Волшебная панель LuCI
Интерфейс доступен в меню **Services -> AmneziaWG**.

- **Кнопка "ЗАПУСТИТЬ VPN"**: Вызывает безопасный скрипт старта.
- **Лог проверки**: На странице выводится хвост файла `/tmp/awg_health.log`, где в реальном времени видно статус Handshake и проверки Google.
- **Авто-восстановление**: Если вы загрузили битый конфиг и нажали "Запуск", через 20 секунд интернет на странице восстановится сам.

## 📝 Настройка параметров маскировки (AmneziaWG 2.0)
Пример корректных параметров в `awg0.conf`:
```ini
Jc = 5
Jmin = 10
Jmax = 50
S1 = 74
S2 = 106
H1 = 1138274689
H2 = 2144085124
H3 = 2147391471
H4 = 2147483115
```
*Примечание: Избегайте пустых значений I1-I5, если они не требуются сервером.*

---
*Документация актуальна на 7 мая 2026 г.*
